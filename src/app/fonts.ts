import { Fira_Code, Roboto_Mono, Alegreya } from "next/font/google";

export const firaCode = Fira_Code({
  subsets: ["latin"],
  display: "swap",
});

export const roboto_mono = Roboto_Mono({
  subsets: ["latin"],
  display: "swap",
});

export const alegreya = Alegreya({
  subsets: ["latin"],
  display: "swap",
});
